# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env*.local
.env.production

# Common ignores
node_modules
.next/
.DS_Store

# Artefatos de build do Firebase Hosting/emuladores — gerado localmente,
# não é código-fonte (chegou a ser commitado sem querer numa tentativa de
# deploy que falhou; ver histórico do git se precisar entender por quê)
.firebase/
firebase-debug.log

# Chaves de segurança do Firebase
firebase-credentials.json